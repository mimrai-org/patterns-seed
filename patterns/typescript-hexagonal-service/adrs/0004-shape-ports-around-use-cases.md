# Shape ports around use cases

Ports expose cohesive capabilities required by application outcomes rather than mirroring complete
database or vendor APIs. Universal interfaces were rejected because they weaken ownership and
substitutability; the trade-off is that distinct use cases may need several small ports over one
external system.
